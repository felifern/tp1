#ifndef OPERADOR_H
#define OPERADOR_H

#include "racional.h"
#include <string.h>
#include <stdio.h>

/*
typedef struct{
    char *operador;
    racional_t *(*funcion) (const racional_t *a, const racional_t *b);
    int aridad;
    int prioridad;
    char *descripcion;
}operador_t;
*/

#endif